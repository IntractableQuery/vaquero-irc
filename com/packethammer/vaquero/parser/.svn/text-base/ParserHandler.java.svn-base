/*
 * This defines a class which handles event parsing based off a call from 
 * elsewhere. It is used exclusively inside the IRC parser.
 */

package com.packethammer.vaquero.parser;

import com.packethammer.vaquero.util.protocol.IRCRawLine;
import com.packethammer.vaquero.parser.events.IRCEvent;

public class ParserHandler {
    /**
     * Attempts to build an event given an IRC line. Returns null if unable
     * to do so.
     */
    public IRCEvent pullEvent(IRCRawLine line) {
        return null;
    }
}
