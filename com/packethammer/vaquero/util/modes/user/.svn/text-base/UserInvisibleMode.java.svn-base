/*
 * Marks a user as invisible.
 *
 * Derived from rfc1459.
 */

package com.packethammer.vaquero.util.modes.user;

public class UserInvisibleMode extends UserMode {
    public UserInvisibleMode() {
        this.setMode(UserMode.MODE_INVISIBLE);
    }
}
